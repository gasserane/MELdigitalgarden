# MELdigitalgarden
a place to track my explorations on MEL topics

## Claude Code skills

Skills are pinned in `skills-lock.json` and not vendored. Claude Code auto-restores them on session start via a SessionStart hook in `.claude/settings.json` (runs `npx skills experimental_install`). To restore manually outside Claude Code, run the same command at the repo root.
